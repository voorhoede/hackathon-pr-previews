{
  "name": "hackathon-pr-previews",
  "version": "1.0.0",
  "main": "main.mjs",
  "type": "module",
  "scripts": {
    "build": "esbuild src/main.mjs --bundle --outfile=dist.mjs --format=esm --platform=node --target=node16",
    "lint": "eslint ./src/* --fix",
    "prepare": "husky install"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/voorhoede/hackathon-pr-previews.git"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^1.9.0",
    "@actions/github": "^5.0.3"
  },
  "devDependencies": {
    "esbuild": "^0.14.48",
    "eslint": "^8.19.0",
    "husky": "^8.0.1"
  }
}
